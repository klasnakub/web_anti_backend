# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# BigQuery Configuration (these are already set in the code, but can be overridden)
# PROJECT_ID=practise-bi
# DATASET_NAME=user
# TABLE_NAME=users

# Application Configuration
# PORT=8080
# HOST=0.0.0.0 